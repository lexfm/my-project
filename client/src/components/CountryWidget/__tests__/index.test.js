import { render, cleanup } from "@testing-library/react";

afterEach(cleanup)

describe("Country Widget", () => {
  if("Renders the default component", () => {})
  if("Renders the first set number of items", () => {})
})